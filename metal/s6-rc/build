#!/bin/sh -e

./configure \
    --prefix=/usr \
    --libexecdir=/usr/lib \
    --disable-static \
    --disable-allstatic \
    --disable-static-libc \
    --enable-shared

make
make DESTDIR="$1" install
