#!/bin/sh -e

./configure \
    --prefix=/ \
    --without-xmlwf \
    --without-examples \
    --without-tests \
    --without-docbook

make
make install

# Remove documentation (Changelogs, etc).
rm -rf "${1:?}/share"
